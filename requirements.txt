# Runtime dependency. ToolpathLab deliberately keeps this list at one entry:
# the front-end ships as static files (three.js is vendored under
# toolpath_lab/web/vendor), so there is no npm install and no build step.
numpy>=1.24
